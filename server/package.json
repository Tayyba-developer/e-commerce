{
  "name": "ecommerce-frontend-demo-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Reference API server matching the Phase 9 REST contract, used to preview the frontend before the real Express + SQL backend is wired in.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.35.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0"
  }
}
